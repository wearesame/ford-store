<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= title %>
    </title>

    <!-- ใช้ Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>

<body class="bg-gray-50 font-sans">

    <!-- ค้นหา -->
    <div class="container mx-auto p-6 -mb-10">
        <input type="text" id="searchInput" class="border border-gray-300 rounded-md p-2 mb-4 w-full"
            placeholder="ค้นหาชื่อผู้ใช้หรือเบอร์โทร..." />
    </div>

    <!-- ตารางแสดงข้อมูล -->
    <div class="container mx-auto p-6">
        <table class="min-w-full bg-white border border-gray-200 rounded-lg shadow-sm overflow-hidden">
            <thead class="bg-gray-100">
                <tr>
                    <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">#</th>
                    <th class="py-3 px-4 text-left text-sm font-medium text-gray-600"></th>
                    <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">ชื่อผู้ใช้</th>
                    <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">ชื่อ-สกุล</th>
                    <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">เบอร์โทรติดต่อ</th>
                    <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">เข้าร่วมเมื่อ</th>
                    <th class="py-3 px-4 text-left text-sm font-medium text-gray-600">จัดการ</th>
                </tr>
            </thead>
            <tbody id="userTableBody">
                <% users.forEach((user, index)=> { %>
                    <tr class="hover:bg-gray-50 transition-all">
                        <td class="py-3 px-4 border-t text-gray-600 text-sm">
                            <%= index + 1 %>
                        </td>

                        <td class="py-3 px-4 border-t text-gray-600 text-sm">
                            <img class="border w-10 h-10 rounded-lg object-cover" src="<%= user.avatar %>" alt="">
                        </td>

                        <td class="py-3 px-4 border-t text-gray-600 text-sm">
                            <%= user.username %>
                        </td>
                        <td class="py-3 px-4 border-t text-gray-600 text-sm">
                            <%= user.fname %>
                                <%= user.lname %>
                        </td>
                        <td class="py-3 px-4 border-t text-gray-600 text-sm">
                            <%= user.phone %>
                        </td>
                        <td class="py-3 px-4 border-t text-gray-600 text-sm">
                            <%= new Date(user.createdAt).toLocaleDateString('th-TH', { weekday: 'long' , year: 'numeric'
                                , month: 'long' , day: 'numeric' }) %>
                        </td>
                        <td class="py-3 px-4 border-t">
                            <!-- ปุ่มลบ -->
                            <form action="/dashboard/users/delete/<%= user.id %>" method="POST" class="inline">
                                <button type="submit" class="btn-danger"
                                    onclick="return confirm('Are you sure you want to delete this user?')"><i
                                        class="ph ph-trash"></i></button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>

    <script>
        // ฟังก์ชันค้นหาด้วย jQuery
        $('#searchInput').on('input', function () {
            var searchTerm = $(this).val().toLowerCase();  // ค่าคำค้นหา
            $('#userTableBody tr').each(function () {
                var rowText = $(this).text().toLowerCase();  // ข้อความในแถว
                if (rowText.indexOf(searchTerm) === -1) {
                    $(this).hide();  // ซ่อนแถวที่ไม่ตรงกับคำค้นหา
                } else {
                    $(this).show();  // แสดงแถวที่ตรงกับคำค้นหา
                }
            });
        });
    </script>

</body>

</html>